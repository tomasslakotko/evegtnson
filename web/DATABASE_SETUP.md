# Инструкция по настройке облачной базы данных

## ✅ Автоматическая настройка (Рекомендуется)

Я уже настроил подключение к вашему Supabase проекту! 

**Осталось только:**
1. Найдите ваш пароль базы данных в Supabase (Settings → Database → Database password)
2. Запустите скрипт из корневой папки проекта:
   ```bash
   ./setup-supabase.sh ВАШ_ПАРОЛЬ
   ```

Скрипт автоматически:
- Обновит `.env` файл
- Применит миграции к базе данных
- Сгенерирует Prisma Client

---

## 🚀 Ручная настройка (если нужно)

### Шаг 1: Создайте проект на Supabase
1. Перейдите на https://supabase.com
2. Нажмите "Start your project" 
3. Войдите через GitHub или создайте аккаунт
4. Нажмите "New Project"
5. Заполните:
   - **Name**: bookthecall
   - **Database Password**: придумайте и сохраните пароль!
   - **Region**: выберите ближайший (например, Europe West)
6. Нажмите "Create new project" и подождите 1-2 минуты

### Шаг 2: Получите connection string
1. В проекте перейдите: **Settings** → **Database**
2. Найдите секцию **Connection string**
3. Выберите вкладку **URI**
4. Скопируйте строку (примерно такую):
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```

### Шаг 3: Обновите конфигурацию
1. Откройте `web/prisma/schema.prisma`
2. Измените строку:
   ```prisma
   provider = "sqlite"  // было
   provider = "postgresql"  // станет
   ```

3. Откройте файл `.env` в папке `web`
4. Замените `DATABASE_URL` на:
   ```
   DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@db.xxxxx.supabase.co:5432/postgres?sslmode=require"
   ```
   ⚠️ Замените `[YOUR-PASSWORD]` на ваш реальный пароль из шага 1

### Шаг 4: Примените миграции
```bash
cd web
npx prisma migrate deploy
npx prisma generate
npm run dev
```

---

## 📋 Альтернативные варианты

### Neon (neon.tech) - тоже бесплатно
1. https://neon.tech → Sign up
2. Create project
3. Copy connection string
4. Обновите `.env` и `schema.prisma` как выше

### Railway (railway.app)
1. https://railway.app → Sign up
2. New Project → Add PostgreSQL
3. Copy connection string из Variables
4. Обновите конфигурацию

---

## ✅ После настройки

Ваша база данных будет в облаке, доступна из любой точки мира, и не будет проблем с локальными файлами!
