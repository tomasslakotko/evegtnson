# Где найти правильный connection string в Supabase

## ❌ НЕ ЭТА ВКЛАДКА!
Вкладка "MCP" - это для интеграции с AI-инструментами, не для базы данных.

## ✅ НУЖНА ВКЛАДКА "Connection String"

### Шаг 1: Переключитесь на вкладку "Connection String"
В верхней части модального окна нажмите на вкладку **"Connection String"** (первая вкладка слева)

### Шаг 2: Выберите Session mode
1. В выпадающем списке **"Method"** выберите **"Session mode"** (не Direct connection)
2. Это важно для IPv4 совместимости!

### Шаг 3: Скопируйте connection string
Вы увидите строку примерно такого вида:
```
postgresql://postgres.lhhbifimethvjtgkepyb:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

### Шаг 4: Обновите .env файл
1. Откройте файл `web/.env`
2. Найдите строку `DATABASE_URL`
3. Замените её на скопированную строку (замените `[YOUR-PASSWORD]` на ваш пароль: `dusnoc-paVpe1-kuxjus`)
4. Сохраните файл

### Шаг 5: Примените миграции
```bash
cd web
npx prisma migrate deploy
npx prisma generate
npm run dev
```

---

## 📸 Что должно быть видно:

После переключения на вкладку "Connection String" вы должны увидеть:
- **Type**: URI
- **Source**: Primary Database  
- **Method**: Session mode (выберите это!)
- **Connection string**: длинная строка с `postgresql://...`

Это и есть то, что нам нужно! 🎯

